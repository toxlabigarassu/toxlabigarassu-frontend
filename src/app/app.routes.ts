import { Routes } from '@angular/router';
import { HomePageComponent } from './features/pages/home/home.page';

export const routes: Routes = [
  {
    path: '',
    component: HomePageComponent
  }
];
